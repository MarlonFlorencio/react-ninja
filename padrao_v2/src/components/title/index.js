'use strict'

import React from 'react'
import './title.css'

const Title = ({ title }) => (
  <h1 className='title'>
    TTTTT
  </h1>
)

Title.propTypes = {
  title: React.PropTypes.string.isRequired
}

export default Title
